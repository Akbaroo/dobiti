# اصول نام‌گذاری کامیت‌ها (Commit Naming Conventions)

برای داشتن یک تاریخچه کامیت تمیز، خوانا و قابل‌درک، بهتر است از یک استاندارد مشخص برای نام‌گذاری کامیت‌ها استفاده کنیم. در ادامه چند روش استاندارد آورده شده است:

---

## ۱. فرمت پیشنهادی (Conventional Commits)

این روش یک استاندارد محبوب است که در پروژه‌های متن‌باز و تیمی استفاده می‌شود.

```
<type>(<scope>): <description>
```

- **`type`** (نوع تغییر): مشخص می‌کند که این کامیت چه کاری انجام داده است.
- **`scope`** (دامنه تغییرات، اختیاری): مشخص می‌کند که تغییرات مربوط به کدام بخش از پروژه است.
- **`description`** (توضیح مختصر): توضیح کوتاهی درباره تغییرات.

### مثال‌ها:
```
feat(auth): add JWT authentication
fix(api): resolve bug in user registration
refactor(database): optimize query performance
```

---

## ۲. انواع `type` ها

| نوع          | توضیح |
|-------------|-------|
| `feat`      | اضافه شدن یک ویژگی جدید |
| `fix`       | رفع باگ |
| `docs`      | تغییرات در مستندات |
| `style`     | تغییرات ظاهری در کد (بدون تغییر در منطق) |
| `refactor`  | بهبود ساختار کد بدون تغییر در رفتار آن |
| `perf`      | بهبود عملکرد کد |
| `test`      | اضافه یا اصلاح تست‌ها |
| `chore`     | تغییرات جزئی که به ویژگی‌ها مربوط نیستند (مانند تغییرات CI/CD) |

---

## ۳. نکات مهم
✅ از زمان حال ساده استفاده کنید.  
✅ توضیح کامیت باید کوتاه و مفید باشد.  
✅ از حروف کوچک برای `type` و `scope` استفاده کنید.  
✅ در صورت نیاز، توضیحات بیشتری در بخش توضیحات (body) بنویسید.

### مثال با توضیحات بیشتر:
```
fix(auth): resolve token expiration issue

The token was expiring too soon due to incorrect expiration settings.
Updated the configuration to extend the validity period.
```

---

## ۴. نمونه کامیت‌های نامناسب

❌ `Fixed a bug` → مشخص نیست چه باگی!  
❌ `Update code` → چه تغییری؟ در کدام بخش؟  
❌ `Some changes` → اصلاً معنی خاصی ندارد!

---

با رعایت این اصول، تاریخچه کامیت‌ها خواناتر و مدیریت آن‌ها آسان‌تر خواهد شد.
