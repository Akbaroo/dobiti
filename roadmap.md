<div dir='rtl'>

# لیست کارهای انجام‌شده و برنامه‌ریزی‌شده برای پروژه

## کارهای انجام‌شده:

- [x] **مدل Post**:
   - ایجاد فیلدهای زیر برای ذخیره اطلاعات پست‌ها:
     - `title`: عنوان پست
     - `content`: محتوای پست
     - `created_at`: تاریخ ایجاد پست
     - `jalali_created_at`: تاریخ ایجاد پست به صورت شمسی
     - `image`, `video`, `audio`: برای آپلود رسانه‌ها (تصاویر، ویدیوها و فایل‌های صوتی)
   
- [x] **مدل User**:
   - شخصی‌سازی مدل پیش‌فرض `User` برای استفاده از مدل `User` مخصوص اپ `accounts`.

- [x] **صفحه جزئیات پست**:
   - ایجاد صفحه‌ای برای نمایش جزئیات پست‌ها که قابلیت نمایش تصاویر، ویدیوها و فایل‌های صوتی را دارد.
   - اضافه کردن قابلیت پخش فایل‌های صوتی با تگ `<audio>` و پخش ویدیوها با تگ `<video>`.

- [x] **سیستم**
