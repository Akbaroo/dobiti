<div dir='rtl'>

# لیست کارهای انجام‌شده و برنامه‌ریزی شده برای پروژه


- [x] **مدل Post**:
   - فیلدهایی برای ذخیره اطلاعات پست‌ها ایجاد شد:
     - `title` (عنوان)
     - `content` (محتوا)
     - `created_at` (تاریخ ایجاد)
     - `jalali_created_at` (تاریخ ایجاد شمسی)
     - `image`, `video`, `audio` برای آپلود رسانه‌ها (تصاویر، ویدیوها و فایل‌های صوتی)
   
- [x] **مدل User**:
   - تغییر مدل پیش‌فرض `User` برای استفاده از مدل `User` شخصی‌سازی‌شده در اپ `accounts`.

- [x] **صفحه جزئیات پست**:
   - صفحه‌ای برای نمایش پست‌ها با قابلیت نمایش تصاویر، ویدیوها و فایل‌های صوتی اضافه شد.
   - قابلیت پخش فایل‌های صوتی با تگ `<audio>` و فایل‌های ویدیویی با تگ `<video>` اضافه شد.

- [x] **سیستم لایک‌ها**:
   - قابلیت لایک کردن پست‌ها به هر پست اضافه شد.

- [x] **تنظیمات مدیریت فایل‌ها**:
   - تنظیمات لازم برای ذخیره و دسترسی به فایل‌های رسانه‌ای در `settings.py` اعمال شد.
   - تنظیمات URL برای دسترسی به فایل‌های رسانه‌ای اضافه شد.

- [ ] **رفع مشکلات فایل صوتی**:
   - اطمینان از پخش صحیح فایل‌های صوتی و عقب و جلو کردن آن‌ها.
   - بررسی فرمت‌های مختلف صوتی (MP3، OGG).

- [ ] **امنیت**:
   - محدود کردن انواع فایل‌های قابل آپلود (برای جلوگیری از فایل‌های مخرب).
   - اعتبارسنجی و اعتبارسنجی فایل‌ها برای جلوگیری از مشکلات امنیتی.

- [ ] **بهینه‌سازی عملکرد**:
   - بهینه‌سازی کوئری‌ها برای بهبود سرعت بارگذاری پست‌ها.
   - استفاده از کش برای بهبود عملکرد صفحات پر بازدید.

- [x] **اضافه کردن قابلیت نظرات**:
   - پیاده‌سازی سیستم نظرات برای پست‌ها.
   - قابلیت نمایش نظرات و اضافه کردن آن‌ها برای هر پست.

- [ ] **مدیریت کاربران**:
   - اضافه کردن پروفایل‌های شخصی برای کاربران.

- [ ] **آزمون و بررسی**:
   - نوشتن تست‌های واحد (Unit Tests) برای اطمینان از عملکرد صحیح بخش‌های مختلف.
   - آزمایش ویژگی‌ها در مرورگرها و دستگاه‌های مختلف.

- [ ] **UI/UX بهبود**:
   - بهبود طراحی و تجربه کاربری (UX) برای صفحات پست‌ها و صفحات مدیریت.

</div>